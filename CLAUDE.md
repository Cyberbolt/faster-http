# 项目规则

## 基本规范
- **语言使用**：用中文回答问题，代码注释使用英文
- **文档查询优先级**：context7 文档查询 > 互联网搜索
- **问题解决流程**：不明确时先查 context7 文档 → 仍无法解决再搜索互联网

## 项目架构原则
**目标**：构建 httpx 的高性能 Rust 替代方案。

### Python 层职责
- **仅作接口层**：对外暴露完整的 httpx 兼容接口
- **最小化逻辑**：除必要情况外，不实现业务逻辑
- **接口验证**：使用 `uv run python -c` 验证 httpx 真实接口行为
- **最佳实践遵循**：严格按照 Python 最佳实践组织代码结构和编写代码

### Rust 层职责  
- **接口转换**：Python 请求参数 → reqwest 请求格式
- **请求处理**：委托 reqwest 执行 HTTP 操作
- **响应转换**：reqwest Response → httpx 兼容的 Python Response 对象
- **最小化实现**：除必要情况外，将逻辑交给 reqwest 处理
- **最佳实践遵循**：严格按照 Rust 最佳实践组织代码结构和编写代码

### 性能要求
- Python-Rust 调用追求极致性能优化
- 减少不必要的数据转换和内存拷贝

### 与 httpx 的区别
- httpx 会自动读取环境变量中的 proxy，但是 faster-http 不会
- httpx 会受 Python GIL 的影响，但是 faster-http 即便在 Python 单线程中，也会在 Rust 端自动利用多核 CPU

## 项目结构最佳实践

### 目录结构规范
- **项目根目录**：遵循混合语言项目最佳实践，清晰分离 Python 和 Rust 代码
- **子目录分层**：严格按照语言特定的最佳实践组织子目录结构
- **模块划分**：功能模块划分清晰，避免循环依赖
- **配置文件**：统一放置在项目根目录，遵循标准命名规范

### Python 目录结构
- 遵循 Python 项目标准结构最佳实践
- 包结构清晰，模块职责单一
- `__init__.py` 文件合理使用，控制模块导入
- 类型标注文件（.pyi）按标准规范放置

### Rust 目录结构  
- 严格遵循 Cargo 项目标准结构
- `src/` 目录按功能模块合理划分
- `lib.rs` 和模块文件遵循 Rust 命名约定
- 依赖声明在 `Cargo.toml` 中规范管理

## 文档依赖

### 开发标准规范

#### 开发规范

##### 代码质量要求
- 编写生产级别代码，严格按照最佳实践
- **Python 代码**：遵循 PEP 8 规范，使用类型标注，合理使用 docstring
- **Rust 代码**：遵循 Rust 官方风格指南，合理使用 documentation comments

##### 项目管理
- 使用 `uv` 管理项目依赖和环境
- 执行模块：`uv run -m` 替代 `python -m`
- 添加依赖：`uv add` 添加生产依赖，`uv add xxx --dev` 添加开发依赖
- 不熟悉 uv 用法时，使用 context7 查询文档

##### 代码检查与编译
- Python 代码修改后用 `ruff` 检查代码质量
- Rust 代码修改后运行 `uv run maturin develop --release` 重新编译
- 代码提交前必须通过所有静态检查工具

##### 临时代码执行
- 验证想法时使用：`uv run python -c "代码内容"`
- **严禁在项目根目录创建临时文件**

##### 任务执行限制
- **禁止运行任何无限阻塞的任务**（如 server 等）
- 所有长时间运行的任务必须添加 `timeout` 限制

##### CI/CD 配置
- `benchmarks` 目录不参与 CI/CD 流程

#### 测试规范

##### 测试覆盖与结构
- 测试代码必须覆盖项目全部功能
- 测试文件统一放置在项目根目录的 `tests/` 目录
- `tests/` 目录按功能模块划分清晰的子目录结构，遵循测试目录最佳实践
- 创建单元测试和集成测试两个层级

##### 兼容性测试流程
- 先用 `httpx` 实现测试
- 再用 `faster-http` 运行相同测试进行对比
- 如发现 `faster-http` 接口不兼容 `httpx`，优先修改源码适配
- `faster-http` 不得实现 `httpx` 不支持的接口

##### 测试独立性
- **测试不得依赖任何外部网站或服务**(如 httpbin.org、nginx:21000 等外部已存在的服务)
- **允许使用内部测试服务器**：测试框架可以启动TestServer等内部HTTP服务器，随测试启停
- 所有测试必须能在内部环境完全运行
- 测试支持并行运行，按并行测试架构来设计

##### 测试代码管理
- 新建测试时复用现有测试结构和代码，避免重复
- 代码更新后及时更新对应测试，删除过时测试
- 保持测试逻辑完备且精简，无冗余代码
- **严禁在项目根目录放置测试文件**

##### 测试执行
- 并发运行全部测试：`uv run -m pytest tests/ -n auto`
- 不能让运行全部测试的时间超过5分钟，即便是写 TDD，也应该立刻报错，严禁让测试阻塞(都是本地测试，不可能阻塞)
- 测试通过率必须保持 100%
- 测试失败时立即排查代码实现或测试代码问题并修复

#### 性能测试规范

##### 测试环境
- 性能测试代码位于 `benchmarks/` 目录
- 性能测试可以使用外部服务（仅限benchmarks目录）
- **普通功能测试禁止访问此服务**

##### 执行命令
```bash
sh scripts/auto_taskset.sh "timeout 60s uv run benchmarks/faster_http_test.py"
```

##### 执行要求
- 使用 `scripts/auto_taskset.sh` 限制 CPU 核心数确保测试公平性
- 必须添加 `timeout` 避免进程阻塞
- 性能测试与功能测试严格分离

### 文档编写标准

#### 目录结构
- 所有文档统一存放在 `docs` 目录下
- 按照最佳实践组织子目录结构，确保文档分类清晰、层次合理

#### 多语言支持
- **主要语言**：英文文档作为默认版本
- **中文支持**：同步提供中文文档
- **版本同步**：任一语言版本更新时，必须同步更新另一语言版本，确保内容一致性

#### 导入推荐
- 推荐用户使用以下方式导入项目：
  ```python
  import faster_http as http
  ```

## 技术栈文档查询
- **httpx**：不确定时使用 context7 查询 + 实际验证
- **reqwest**：不确定时使用 context7 查询相关文档