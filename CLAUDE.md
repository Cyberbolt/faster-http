# 项目规则

## 基本规范
- **语言使用**：用中文回答问题，代码注释使用英文
- **文档查询优先级**：context7 文档查询 > 互联网搜索
- **问题解决流程**：不明确时先查 context7 文档 → 仍无法解决再搜索互联网

## 项目架构原则
**目标**：构建 httpx 的高性能 Rust 替代方案

### Python 层职责
- **仅作接口层**：对外暴露完整的 httpx 兼容接口
- **最小化逻辑**：除必要情况外，不实现业务逻辑
- **接口验证**：使用 `uv run python -c` 验证 httpx 真实接口行为

### Rust 层职责  
- **接口转换**：Python 请求参数 → hyper 请求格式
- **请求处理**：委托 hyper 执行 HTTP 操作
- **响应转换**：hyper Response → httpx 兼容的 Python Response 对象
- **最小化实现**：除必要情况外，将逻辑交给 hyper 处理

### 性能要求
- Python-Rust 调用追求极致性能优化
- 减少不必要的数据转换和内存拷贝

### 与 httpx 的区别
- httpx 会自动读取环境变量中的 proxy，但是 faster-http 不会
- httpx 会受 Python GIL 的影响，但是 faster-http 即便在 Python 单线程中，也会在 Rust 端自动利用多核 CPU

## 文档依赖

### 开发标准规范

#### 开发规范

##### 代码质量要求
- 编写生产级别代码，严格按照最佳实践

##### 项目管理
- 使用 `uv` 管理项目依赖和环境
- 执行模块：`uv run -m` 替代 `python -m`
- 添加依赖：`uv add` 添加生产依赖，`uv add xxx --dev` 添加开发依赖
- 不熟悉 uv 用法时，使用 context7 查询文档

##### 代码检查与编译
- Python 代码修改后用 `ruff` 检查代码质量
- Rust 代码修改后运行 `uv run maturin develop --release` 重新编译

##### 临时代码执行
- 验证想法时使用：`uv run python -c "代码内容"`
- **严禁在项目根目录创建临时文件**

##### 任务执行限制
- **禁止运行任何无限阻塞的任务**（如 server 等）
- 所有长时间运行的任务必须添加 `timeout` 限制

##### CI/CD 配置
- `benchmarks` 目录不参与 CI/CD 流程

#### 测试规范

##### 测试覆盖与结构
- 测试代码必须覆盖项目全部功能
- 测试文件统一放置在项目根目录的 `tests/` 目录
- `tests/` 目录按功能模块划分清晰的子目录结构
- 创建单元测试和集成测试两个层级

##### 兼容性测试流程
- 先用 `httpx` 实现测试
- 再用 `faster-http` 运行相同测试进行对比
- 如发现 `faster-http` 接口不兼容 `httpx`，优先修改源码适配
- `faster-http` 不得实现 `httpx` 不支持的接口

##### 测试独立性
- **测试不得依赖任何外部网站或服务**(如 httpbin.org 等)
- 所有测试必须能在内部环境完全运行
- 测试支持并行运行，按并行测试架构来设计

##### 测试代码管理
- 新建测试时复用现有测试结构和代码，避免重复
- 代码更新后及时更新对应测试，删除过时测试
- 保持测试逻辑完备且精简，无冗余代码
- **严禁在项目根目录放置测试文件**

##### 测试执行
- 并发运行全部测试：`uv run -m pytest tests/ -n auto`
- 不能让运行全部测试的时间超过5分钟，即便是写 TDD，也应该立刻报错，严禁让测试阻塞(都是本地测试，不可能阻塞)
- 测试通过率必须保持 100%
- 测试失败时立即排查代码实现或测试代码问题并修复

#### 性能测试规范

##### 测试环境
- 性能测试代码位于 `benchmarks/` 目录
- 性能测试可以使用外部服务（仅限benchmarks目录）
- **普通功能测试禁止访问此服务**

##### 执行命令
```bash
sh scripts/auto_taskset.sh "timeout 60s uv run benchmarks/faster_http_test.py"
```

##### 执行要求
- 使用 `scripts/auto_taskset.sh` 限制 CPU 核心数确保测试公平性
- 必须添加 `timeout` 避免进程阻塞
- 性能测试与功能测试严格分离

### 文档编写标准

#### 目录结构
- 所有文档统一存放在 `docs` 目录下
- 按照最佳实践组织子目录结构，确保文档分类清晰、层次合理

#### 多语言支持
- **主要语言**：英文文档作为默认版本
- **中文支持**：同步提供中文文档
- **版本同步**：任一语言版本更新时，必须同步更新另一语言版本，确保内容一致性

#### 导入推荐
- 推荐用户使用以下方式导入项目：
  ```python
  import faster_http as http
  ```

## 技术栈文档查询
- **httpx**：不确定时使用 context7 查询 + 实际验证
- **hyper**：不确定时使用 context7 查询相关文档